---
title: AudioSegmentService
description: API documentation generated from code docstrings
sidebar_position: 100
---
# AudioSegmentService

> **Note**: This documentation is automatically generated from code docstrings in the main LangQuest repository.

[**langquest v2.0.10**](/docs/developers/..-..-..-index)

***

[langquest](/docs/developers/..-..-..-modules) / [database\_services/audioSegmentService](/docs/developers/..-index) / AudioSegmentService

# Class: AudioSegmentService

Defined in: [database\_services/audioSegmentService.ts:18](https://github.com/genesis-ai-dev/langquest/blob/0ad4fae4cdf57c04fd8ccdbaf8ce236b52fc089b/database_services/audioSegmentService.ts#L18)

## Constructors

### Constructor

> **new AudioSegmentService**(): `AudioSegmentService`

#### Returns

`AudioSegmentService`

## Methods

### saveAudioSegment()

> **saveAudioSegment**(`segment`, `questId`, `sourceLanguageId`, `creatorId`, `projectId`): `Promise`\<\{ `assetId`: `string`; `audioUri`: `string`; \}\>

Defined in: [database\_services/audioSegmentService.ts:22](https://github.com/genesis-ai-dev/langquest/blob/0ad4fae4cdf57c04fd8ccdbaf8ce236b52fc089b/database_services/audioSegmentService.ts#L22)

Save audio segment to device storage and create asset record

#### Parameters

##### segment

[`AudioSegmentData`](/docs/developers/..-interfaces-AudioSegmentData)

##### questId

`string`

##### sourceLanguageId

`string`

##### creatorId

`string`

##### projectId

`string`

#### Returns

`Promise`\<\{ `assetId`: `string`; `audioUri`: `string`; \}\>

***

### saveAudioSegments()

> **saveAudioSegments**(`segments`, `questId`, `sourceLanguageId`, `creatorId`, `projectId`): `Promise`\<\{ `assetIds`: `string`[]; `audioUris`: `string`[]; \}\>

Defined in: [database\_services/audioSegmentService.ts:96](https://github.com/genesis-ai-dev/langquest/blob/0ad4fae4cdf57c04fd8ccdbaf8ce236b52fc089b/database_services/audioSegmentService.ts#L96)

Save multiple audio segments as a batch

#### Parameters

##### segments

[`AudioSegmentData`](/docs/developers/..-interfaces-AudioSegmentData)[]

##### questId

`string`

##### sourceLanguageId

`string`

##### creatorId

`string`

##### projectId

`string`

#### Returns

`Promise`\<\{ `assetIds`: `string`[]; `audioUris`: `string`[]; \}\>

***

### deleteAudioSegment()

> **deleteAudioSegment**(`assetId`, `options?`): `Promise`\<`void`\>

Defined in: [database\_services/audioSegmentService.ts:149](https://github.com/genesis-ai-dev/langquest/blob/0ad4fae4cdf57c04fd8ccdbaf8ce236b52fc089b/database_services/audioSegmentService.ts#L149)

Delete audio segment and all associated records (including child translations)

Deletion order (children before parents to maintain referential integrity):
1. Find all child assets (translations/transcriptions with source_asset_id = this asset)
2. For each child asset: delete votes, asset_tag_links, quest_asset_links, asset_content_links, then the asset
3. For the parent asset: delete votes, asset_tag_links, quest_asset_links, asset_content_links, then the asset

#### Parameters

##### assetId

`string`

The ID of the asset to delete

##### options?

###### preserveAudioFiles?

`boolean`

If true, skip deleting audio files from attachment queue.
  Use this when merging assets, where audio files are being transferred to another asset.

#### Returns

`Promise`\<`void`\>

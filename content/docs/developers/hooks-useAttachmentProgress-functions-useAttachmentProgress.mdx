---
title: useAttachmentProgress
description: API documentation generated from code docstrings
sidebar_position: 100
---
# useAttachmentProgress

> **Note**: This documentation is automatically generated from code docstrings in the main LangQuest repository.

[**langquest v2.0.11**](/docs/developers/..-..-..-index)

***

[langquest](/docs/developers/..-..-..-modules) / [hooks/useAttachmentProgress](/docs/developers/..-index) / useAttachmentProgress

# Function: useAttachmentProgress()

> **useAttachmentProgress**(`enabled`): `object`

Defined in: [hooks/useAttachmentProgress.ts:69](https://github.com/genesis-ai-dev/langquest/blob/26778d4c97c2a12012367ee23c2613fc07126235/hooks/useAttachmentProgress.ts#L69)

Hook that provides attachment progress tracking with throttling and memoization
to prevent cascading re-renders.

## Parameters

### enabled

`boolean` = `true`

Only query attachment states when enabled (e.g., when drawer is open)

## Returns

`object`

### progress

> **progress**: [`AttachmentProgress`](/docs/developers/..-interfaces-AttachmentProgress)

### syncProgress

> **syncProgress**: \{ `downloading`: `false`; `uploading`: `false`; `downloadCurrent`: `0`; `downloadTotal`: `0`; `uploadCurrent`: `0`; `uploadTotal`: `0`; \} \| \{ `downloading`: `any`; `uploading`: `any`; `downloadCurrent`: `any`; `downloadTotal`: `any`; `uploadCurrent`: `any`; `uploadTotal`: `any`; \}

### isLoading

> **isLoading**: `boolean`

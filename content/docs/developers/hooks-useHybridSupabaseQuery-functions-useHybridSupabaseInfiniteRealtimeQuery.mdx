---
title: useHybridSupabaseInfiniteRealtimeQuery
description: API documentation generated from code docstrings
sidebar_position: 100
---
# useHybridSupabaseInfiniteRealtimeQuery

> **Note**: This documentation is automatically generated from code docstrings in the main LangQuest repository.

[**langquest v2.0.9**](/docs/developers/..-..-..-index)

***

[langquest](/docs/developers/..-..-..-modules) / [hooks/useHybridSupabaseQuery](/docs/developers/..-index) / useHybridSupabaseInfiniteRealtimeQuery

# Function: useHybridSupabaseInfiniteRealtimeQuery()

> **useHybridSupabaseInfiniteRealtimeQuery**\<`T`\>(`__namedParameters`): `object`

Defined in: [hooks/useHybridSupabaseQuery.ts:1107](https://github.com/genesis-ai-dev/langquest/blob/bcf9613481583b05699aeca2003ff5ce69686880/hooks/useHybridSupabaseQuery.ts#L1107)

useHybridSupabaseInfiniteRealtimeQuery

Combines infinite scrolling with realtime subscriptions using offline-first pattern.
Always queries local data first, then cloud data when available, merges with local priority,
and automatically handles cache updates for cloud data when realtime events occur.

## Type Parameters

### T

`T` *extends* `Record`\<`string`, `unknown`\>

## Parameters

### \_\_namedParameters

`HybridSupabaseInfiniteRealtimeQueryOptions`\<`T`\>

## Returns

`object`

### data

> **data**: `object` = `mergedData`

#### data.pages

> **pages**: `HybridPageData`\<`T`\>[] = `mergedPages`

#### data.pageParams

> **pageParams**: `any`

### fetchNextPage()

> **fetchNextPage**: () => `void`

#### Returns

`void`

### fetchPreviousPage()

> **fetchPreviousPage**: () => `void`

#### Returns

`void`

### hasNextPage

> **hasNextPage**: `any`

### hasPreviousPage

> **hasPreviousPage**: `any`

### isFetchingNextPage

> **isFetchingNextPage**: `any`

### isFetchingPreviousPage

> **isFetchingPreviousPage**: `any`

### isLoading

> **isLoading**: `any`

### isError

> **isError**: `any`

### error

> **error**: `any`

### isFetching

> **isFetching**: `any`

### isSuccess

> **isSuccess**: `any` = `localQuery.isSuccess`

### refetch()

> **refetch**: () => `void`

#### Returns

`void`

### status

> **status**: `string`

## Example

```ts
const {
  data,
  fetchNextPage,
  hasNextPage
} = useHybridSupabaseInfiniteRealtimeQuery({
  queryKey: ['assets', 'paginated', questId],
  query: (pageParam) => db.select().from(assetTable).limit(20).offset(pageParam * 20),
  pageSize: 20,
  channelName: 'public:asset',
  subscriptionConfig: {
    table: 'asset',
    schema: 'public'
  },
  getId: (asset) => asset.id,
});
```
